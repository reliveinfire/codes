#include <iostream>
#include <string>
#include <map>
#include <vector>
using namespace std;
class Solution
{
	public:
		int findCircleNum(vector< vector<int> >& M) 
		{
			int size = M.size();
			return size;	
		}
};

int main()
{
	int ret;
	int test[3][3] = {
			{1,1,0},
			{1,1,0},
			{0,0,1}
			};
	vecotor<int> row;
	vector<int> matrix;

	for (int i = 0 ; i < 3 ; i++) {
		for (int j = 0 ; j < 3 ; j++)
	}
	Solution sol;
	cout << test[2][1];
	cout << "\n";
	
	return 0;

}
